/// <reference types="miniprogram-api-typings" />
export interface RealtekTaskParams {
    apSsid: string;
    apBssid: string;
    apPassword: string;
    localIp?: string;
    logger?: any;
}
export interface remoteInfo {
    address: string;
    family: string;
    port: number;
    size: number;
}
export interface RealtekTaskResult {
    isSuc: boolean;
    macStr: string;
    inetAddress: string;
    remoteInfo: remoteInfo;
}
export declare class RealtekTask {
    ssid: string;
    bssid: string;
    password: string;
    localIp: string;
    logger: WechatMiniprogram.Console;
    mTask: any;
    /**
     * Constructor of RealtekTask
     *
     * @param apSsid     the Ap's ssid
     * @param apBssid    the Ap's bssid
     * @param apPassword the Ap's password
     * @param encryptor    加密方式，可以为空
     * @param logger    写日志的方式
     */
    constructor({ apSsid, apBssid, apPassword, localIp, logger, }: RealtekTaskParams);
    start(): Promise<RealtekTaskResult[]>;
    interrupt(): void;
}
