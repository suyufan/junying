export declare class SimpleStore<T> {
    filter: (params: Partial<T>) => (target: T) => boolean;
    _store: T[];
    constructor({ filter, }: {
        filter: (params: Partial<T>) => (target: T) => boolean;
    });
    getAll(): T[];
    get(params: Partial<T>): T;
    set(target: T): number;
    remove(params: Partial<T>): void;
    clear(): void;
}
