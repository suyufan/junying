import EventEmitter from 'event-emitter-for-miniprogram';
import { WebSocket } from '../utils';
import { AppDevSdk } from '../AppDevSdk';
export interface IotWebsocketOptions {
    url?: string;
    heartbeatInterval?: number;
    apiPlatform?: string;
}
export declare class IotWebsocket extends EventEmitter {
    sdk: AppDevSdk;
    requestHandlerMap: any;
    options: IotWebsocketOptions;
    _connected: boolean;
    _subscribeDeviceIdList: string[];
    _heartBeatTimer: number;
    ws: WebSocket;
    _doConnectWsPromise: Promise<void>;
    constructor(sdk: AppDevSdk, options: IotWebsocketOptions);
    isConnected(): boolean;
    doConnectWs(): Promise<void>;
    connect(deviceIdList?: string[]): Promise<void>;
    disconnect(): void;
    send(action: any, params?: {}, { reqId }?: any): Promise<any>;
    sendWsHeatBeat(): Promise<any>;
    activePush(): void;
}
