export declare function queryTokenStateAndBind({ token, productId, deviceName, familyId, roomId, onProgress, sdk, skipBind, // 只校验token，不执行绑定
newTokenVersion, }: {
    token: any;
    productId: any;
    deviceName: any;
    familyId: any;
    roomId: any;
    onProgress: any;
    sdk: any;
    skipBind?: boolean;
    newTokenVersion?: boolean;
}): Promise<{
    productId: any;
    deviceName: any;
}>;
