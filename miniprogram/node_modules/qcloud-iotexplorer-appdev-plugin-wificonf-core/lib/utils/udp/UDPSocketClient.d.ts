import { AppDevSdk } from 'qcloud-iotexplorer-appdev-sdk';
export declare class UDPSocketClient extends AppDevSdk.utils.EventEmitter {
    TAG: string;
    mSocket: any;
    mInterval: any;
    isClose: any;
    dataQueue: {};
    queueExcuteState: {};
    errorHandler: any;
    constructor(port: any);
    checkIsSupport(): boolean;
    interrupt(): void;
    close(): void;
    sendDataSideBySide(data: any, offset: number, count: any, targetIp: any, targetPort: any, interval: any, callback: any): any;
    /**
       * send the data by UDP
       *
       * @param data       the data to be sent
       * @param offset     the offset which data to be sent
       * @param count      the count of the data
       * @param targetPort the port of target
       * @param interval   the milliseconds to between each UDP sent
       */
    sendData(data: any, offset: number, count: any, targetIp: any, targetPort: any, interval: any): Promise<void>;
}
