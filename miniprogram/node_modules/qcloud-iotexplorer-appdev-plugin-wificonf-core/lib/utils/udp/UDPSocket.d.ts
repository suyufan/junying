/**
 * @description airkiss使用的udp通道，先不做统一了
 */
import { AppDevSdk } from 'qcloud-iotexplorer-appdev-sdk';
export declare class UDPSocket extends AppDevSdk.utils.EventEmitter {
    socket: any;
    isClose: boolean;
    destroy: () => void;
    constructor(port: any);
    sendData({ address, port, message, offset, length, timeDelay, }: {
        address: any;
        port: any;
        message: any;
        offset: any;
        length: any;
        timeDelay: any;
    }): Promise<void>;
    sendDataSideBySide({ address, port, data, // data是个数组
    offset, length, timeDelay, callback, }: {
        address: any;
        port: any;
        data?: any[];
        offset: any;
        length: any;
        timeDelay: any;
        callback: any;
    }): any;
}
