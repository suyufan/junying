export const WifiSmartConfigHardwareGuide: "1. 接通设备电源。\n2. 长按复位键（开关）5 秒，指示灯快闪时，再次长按复位键 3 秒。\n3. 指示灯进入慢闪状态即可。";
export const InputDeviceWifiInfoIntroGuide: "1. 接下来您需要将手机WiFi连接到设备热点，设备热点名称如上图所示。";
export const InputDeviceWifiInfoFormGuide: "1. 点击WiFi名称右侧的下拉按钮，选择设备热点。\n2. 填写设备热点密码，若设备热点无密码则无需填写。\n3. 点击下一步，开始配网。";
export const PullWifiListErrorMsg: {
    IOS_REFRESH_TIMEOUT: string;
    DEFAULT: string;
    UDP_ERROR: string;
    UDP_CLOSED: string;
    UDP_SEND_MSG_FAIL: string;
    UDP_NOT_RESPONSE: string;
    CONNECT_WIFI_FAIL: string;
    CONNECT_WIFI_IN_SETTING_ERROR: string;
    CONNECT_WIFI_IN_SETTING_TIMEOUT: string;
    WIFI_CONF_FAIL: string;
    PROTOCOL_FAIL: string;
    PROTOCOL_TIMEOUT: string;
    PROTOCOL_INVALID_RESPONSE: string;
    BUSINESS_DEVICE_ERROR: string;
    BUSINESS_INVALID_RESPONSE: string;
    BUSINESS_DEVICE_CONNECT_MQTT_FAIL: string;
    BUSINESS_DEVICE_CONNECT_WIFI_FAIL: string;
    BUSINESS_ADD_DEVICE_FAIL: string;
    BUSINESS_QUERY_BIND_TOKEN_TIMEOUT: string;
    BUSINESS_QUERY_BIND_TOKEN_FAIL: string;
    BUSINESS_WIFI_RECONNECT_FAIL: string;
    MODULE_REPORT_CONNECT_WIFI_ERROR: string;
    MODULE_REPORT_COMMUNICATE_AP_ERROR: string;
    MODULE_REPORT_ERROR: string;
    MODULE_REPORT_TIMEOUT: string;
};
