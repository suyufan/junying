import { WifiConfProtocolBase } from 'qcloud-iotexplorer-appdev-plugin-wificonf-core';
export declare class SoftAp extends WifiConfProtocolBase {
    udpAddress: string;
    udpPort: number;
    connectAborted: boolean;
    constructor(sdk: any, options: any);
    start({ doNotRetry, isUdpRetry }?: {
        doNotRetry?: boolean;
        isUdpRetry?: boolean;
    }): any;
    doBusiness(): Promise<any>;
}
