export class EsptouchGenerator {
    /**
     * Constructor of EsptouchGenerator, it will cost some time(maybe a bit
     * much)
     *
     * @param apSsid      the Ap's ssid
     * @param apBssid     the Ap's bssid
     * @param apPassword  the Ap's password
     * @param inetAddress the phone's or pad's local ip address allocated by Ap
     */
    constructor(apSsid: any, apBssid: any, apPassword: any, inetAddress: any, encryptor: any);
    mGcBytes2: any;
    mDcBytes2: any;
    getGCBytes2(): any;
    getDCBytes2(): any;
}
