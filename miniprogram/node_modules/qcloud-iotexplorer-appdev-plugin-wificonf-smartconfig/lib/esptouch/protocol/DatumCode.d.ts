export class DatumCode {
    /**
     * Constructor of DatumCode
     *
     * @param apSsid      the Ap's ssid
     * @param apBssid     the Ap's bssid
     * @param apPassword  the Ap's password
     * @param ipAddress   the ip address of the phone or pad
     * @param encryptor null use origin data, not null use encrypted data
     */
    constructor(apSsid: any, apBssid: any, apPassword: any, ipAddress: any, encryptor: any);
    EXTRA_LEN: number;
    EXTRA_HEAD_LEN: number;
    DATA_CODE_LEN: number;
    mDataCodes: any;
    getBytes(): Int8Array;
    toString(): string;
    getU8s(): Uint16Array;
}
