export class EsptouchTask {
    /**
     * Constructor of EsptouchTask
     *
     * @param apSsid     the Ap's ssid
     * @param apBssid    the Ap's bssid
     * @param apPassword the Ap's password
     * @param encryptor    加密方式，可以为空
     * @param logger    写日志的方式
     */
    constructor({ apSsid, apBssid, apPassword, logger, localIp, }: {
        apSsid: any;
        apBssid: any;
        apPassword: any;
        logger: any;
        localIp?: string;
    });
    mTask: any;
    ssid: TouchData;
    bssid: TouchData;
    password: TouchData;
    localIp: string;
    logger: any;
    start(): Promise<any>;
    interrupt(): void;
    isCancelled(): any;
}
import { TouchData } from "../protocol/TouchData";
